<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('../partials/nav.ejs') %>
    <ul>
      <% if (locals.gameInfo) { %> <% gameInfo.forEach(videoGame => { %>
      <li><%= `${videoGame.video_game_name}: [${videoGame.genre_name}]`%></li>
      <div><%= `Publisher: ${videoGame.publisher}`%></div>
      <div><%= `Release Year: ${videoGame.releaseYear}`%></div>
      <form action="/games/<%= videoGame.video_game_name%>/edit" method="GET">
        <button type="submit">Edit</button>
      </form>
      <form action="/games/<%= videoGame.video_game_name%>/delete" method="POST">
        <button type="submit">Delete</button>
      </form>
      <% }); %> <%}%>
    </ul>
  </body>
</html>
